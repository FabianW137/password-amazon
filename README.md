# VoiceAuth REST (eine Domain) — v3
Sauberer Build mit Boot Parent + repackage im Dockerfile.

## Deploy (Render Docker)
- Neues Web Service → Docker → dieses Projekt.
- UI `/`, API `/api/*`, Health `/health`.

## Lokal
mvn spring-boot:run
